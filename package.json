{
  "dependencies": {
    "@prisma/client": "^5.17.0"
  },
  "devDependencies": {
    "prisma": "^5.17.0"
  },
  "scripts": {
"postinstall": "prisma generate",
    "start": "prisma generate && fastapi run"
  }

}
