{
  "dependencies": {
    "@prisma/client": "^5.17.0"
  },
  "devDependencies": {
    "prisma": "^5.17.0"
  },
  "scripts": {
  "build": "prisma install && npm run prisma:generate && prisma run prisma:migrate",
  "postinstall": "prisma generate",
    "prisma:generate": "prisma generate",
  "prisma:migrate": "prisma migrate dev"
}

}
